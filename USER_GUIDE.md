# Research Starter 使用指南

## 开始使用

1. 完整解压从 GitHub Releases 下载的 Windows ZIP。
2. 双击带应用图标的 **Research Starter.exe**。如果启动器无法运行，可以尝试备用入口 `Start Research Starter.cmd`。
3. 浏览器打开 Research Starter 首页后，进入 **Settings** 完成账户和可选路径设置。

软件已经包含运行所需的 Python、Node.js 和项目依赖，不需要另外安装或选择这些运行环境。

首页按 Paper Guide、Research Note 和 Research Slides 分类显示项目。首次启动时会看到三个不消耗模型额度的完成示例，可用来熟悉阅读、预览和下载界面。过去的项目可以重新打开、改名、加星或删除。

## 添加素材

三个功能的输入页都支持拖放素材，也可以点击上传区域从电脑选择。

- Paper Guide 选择一份 PDF。
- Research Note 选择一个素材文件夹。
- Research Slides 选择一个素材文件夹，并可另外选择 PPTX 模板。

上传后的内容会出现在项目 Inbox 中，之后可以直接通过页面下拉框再次选择。发行包自带的 `public-sample` 是可重复使用的测试素材。

## 选择模型

任务页可以选择 **OpenAI Codex**、**DeepSeek API** 或 **Kimi API**，并为当前任务设置模型和推理等级。

### OpenAI Codex

在 **Settings** 中查看账户状态。尚未登录时，点击“登录 / 重新登录其他账户”，在打开的 OpenAI 页面完成登录后返回软件。任务页会显示当前账户可用的模型与用量信息。

### DeepSeek API

在 **Settings** 的 DeepSeek 区域填写 API Key，保存后点击“测试连接”。连接成功后即可在任务页选择 DeepSeek。使用费用和余额请在 DeepSeek 平台查看。

### Kimi API

在 **Settings** 的 Kimi 区域填写 API Key，保存后点击“测试连接”。连接成功后即可在任务页选择 Kimi。使用费用和余额请在 Kimi 开放平台查看。

DeepSeek 和 Kimi 可以独立使用，不要求登录 Codex。API Key 会加密保存在当前电脑上，界面不会显示完整密钥。

## Paper Guide：阅读和讨论论文

1. 拖入或选择论文 PDF。
2. 根据需要填写阅读重点；不需要补充时可以留空。
3. 选择输出语言、模型和推理等级，然后点击“开始阅读”。
4. 导读完成后，左侧显示对话，右侧显示 PDF。
5. 阅读 PDF 的同时，可以继续询问论文内容、页码、图表和公式。

对话区支持常见 LaTeX 公式。后续提问前可以调整模型和推理等级，新的选择会从下一次发送开始使用。

## Research Note：整理并保存研究笔记

1. 拖入或选择包含文字、图片、动图等材料的文件夹。
2. 根据需要填写整理重点或笔记要求；补充说明可以留空。
3. 选择输出语言、模型和推理等级，然后生成预览。
4. 检查笔记的正文、图片和动图。
5. 如需调整，在“补充修改建议”中输入要求并生成新的预览。可以重复修改，直到结果合适。

确认内容后，按以下顺序保存：

1. 点击“确认并写入本地 Obsidian”。
2. 点击“在 Obsidian 中打开本地笔记”，检查正文和附件是否正常显示。
3. 如果已经在 Settings 配置云端 Obsidian，可在检查完成后点击“确认写入云端 Obsidian”。

本地笔记和云端笔记都会连同相关附件一起保存。不同任务会保存为独立笔记，不会自动与以前的同名笔记合并。

## Research Slides：生成和修改演示文稿

页面左侧用于选择模板，右侧用于选择素材、输出语言和 PPT 生成模型。“内容 / 页面逻辑”可以留空；如果素材文件夹中已有 Markdown 或 TXT 说明文件，软件会读取其中的内容。

### 不使用模板

直接选择素材文件夹并生成 PPT。未选择模板时，默认使用简洁的学术演示风格。

### 使用已有模板

从模板库选择标有“已载入”的模板。选择后可以在页面左侧预览，再使用该模板生成 PPT。

### 添加新模板

1. 拖入一份 PPTX 模板。
2. 为模板载入选择模型和推理等级。建议使用能力较强的模型。
3. 点击“载入所选模板”，等待模板分析完成。
4. 返回 Research Slides 输入页，选择已经载入的模板并预览。
5. 选择素材文件夹、输出语言和 PPT 生成模型，然后生成 PPT。

一份模板完成载入后可以重复使用。如果模板文件内容发生变化，请重新载入。

### 预览、修订和下载

生成完成后，可以在页面中滚动预览 PPTX。在“补充修改建议”中输入要求，可以生成新的完整版本。

使用预览区上方的版本下拉框切换初始版本和历次修订版本；页面底部可以选择对应版本并下载。网页预览与桌面 PowerPoint 的字体或特效可能略有差异，正式使用前建议再用 PowerPoint 检查。

## Settings

### 账户

- 查看或重新登录 Codex 账户。
- 保存并测试 DeepSeek API Key。
- 保存并测试 Kimi API Key。

### 文件位置

可以根据自己的使用环境配置以下位置：

- 外部素材目录。
- 外部成品目录。
- 本地 Obsidian Vault。
- 云端 Obsidian Vault 和笔记目录。
- Obsidian 程序位置。

外部素材、外部成品和云端 Obsidian 都是可选项。没有使用 iCloud 或云端 Vault 时可以留空，不影响本地上传、生成、预览和下载。

路径修改会用于之后创建的任务，不会自动移动以前的项目文件。

## 遇到问题

- **无法读取 Codex 账户或用量**：先在 Settings 刷新状态；尚未登录时使用登录入口。
- **DeepSeek 或 Kimi 无法使用**：检查 API Key 是否已经保存，并重新执行“测试连接”。
- **素材无法选择**：确认文件已经下载到本机，并且原来的文件或文件夹仍然存在。
- **任务失败**：查看任务页面显示的具体错误信息；如果已有可用预览，可先下载保存。
- **PPTX 预览与 PowerPoint 不一致**：下载文件并使用 PowerPoint 检查最终效果。
- **页面显示异常**：先刷新浏览器；如果问题仍然存在，关闭后重新启动 Research Starter。

项目地址：<https://github.com/DingRonghao/research-starter-sdk>
