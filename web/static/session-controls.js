const model=document.querySelector('#session-model'),effort=document.querySelector('#session-effort'),usage=document.querySelector('#session-usage');
let models=[],loaded=false;
export function selection(){return model?.value?{model:model.value,effort:effort.value}:{};}
function updateEfforts(preferred){const selected=models.find(item=>item.model===model.value);effort.replaceChildren();for(const item of selected?.supportedReasoningEfforts||[]){const option=new Option(item.reasoningEffort,item.reasoningEffort);effort.add(option);}effort.value=preferred||selected?.defaultReasoningEffort||'';if(!effort.value&&effort.options.length)effort.selectedIndex=0;}
const limitText=(value,label)=>value&&Number.isFinite(value.usedPercent)?`${label}剩余 ${Math.max(0,100-value.usedPercent)}%`:`${label}用量暂不可用`;
async function refreshUsage(force=false){if(!usage)return;try{const response=await fetch('/api/codex/status'+(force?'?refresh=true':''));if(!response.ok)throw new Error();const data=await response.json(),limits=data.usage?.rateLimits||Object.values(data.usage?.rateLimitsByLimitId||{})[0]||{};usage.textContent=limitText(limits.primary,'5 小时')+' · '+limitText(limits.secondary,'一周');usage.title='最近更新 '+new Date().toLocaleTimeString();if(!loaded){models=(data.models?.data||[]).filter(item=>!item.hidden);if(models.length){model.replaceChildren(...models.map(item=>new Option(item.displayName,item.model)));model.value=window.RESEARCH_JOB.model;if(!model.value)model.value=models.find(item=>item.isDefault)?.model||models[0].model;updateEfforts(window.RESEARCH_JOB.reasoning_effort==='runtime default'?'':window.RESEARCH_JOB.reasoning_effort);loaded=true;}}}catch{usage.textContent='暂时无法读取用量；可在 Settings 检查登录。';}}
model?.addEventListener('change',()=>updateEfforts());document.querySelector('#session-refresh')?.addEventListener('click',()=>refreshUsage(true));
refreshUsage();if(usage)setInterval(()=>{if(!document.hidden)refreshUsage();},60000);
