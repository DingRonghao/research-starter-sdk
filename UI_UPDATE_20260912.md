# 2026-09-12：设置、登录、阅读控制与 PPT 修订

本地开发版本为 0.2.1。按用户新增需求扩展 Web 入口，环境保持既有项目 `.venv`。

- Settings 支持编辑路径、制作者和 GitHub 地址；校验目录和 Vault 写入边界，备份原配置后生效。任务运行中不允许变更配置或开始登录。修改路径不会迁移文件。
- 使用已安装 Codex Python SDK 的 login_chatgpt()/wait()/cancel()，保持连接直至完成、失败、取消或超时。网页不收集密码。启动器不再在启动 Web 服务前阻塞等待登录。
- About 显示版本及用户填写的信息；使用指南来自 USER_GUIDE.md，以本地 Markdown 组件渲染。
- 新增项目 Node 依赖 KaTeX 0.18.7（MIT）：https://katex.org/docs/api 。公式在 Markdown 解析阶段处理，避免反斜杠被提前转义。使用 trust=false 和 DOMPurify。
- Paper Guide 阅读区可查看用量、选择下一次追问的模型与推理等级；同一 thread 续聊。
- Research Slides 在同一 thread 多轮修订，每轮产生唯一命名的新 PPTX，验证 OOXML 后更新最新预览，旧版本保留。修订版不自动覆盖已发布的 iCloud 文件。
- Research Note 的运行时路径说明覆盖历史 Skill 默认路径示例。

验证：23 项 Python 自动测试、8 项 Markdown/KaTeX 解析检查通过；页面、静态资源和健康接口返回 200。真实账户只读检查返回已登录、4 个模型和可用用量数据。

验证边界：账户登录使用模拟 SDK 验证连接生命周期，未注销或替换用户真实账户。PPT 修订使用模拟 Runner 验证两轮状态及文件版本管理，未进行额外真实 Codex 生成。浏览器自动化访问 localhost 被 ERR_BLOCKED_BY_CLIENT 拦截，尚未完成本轮浏览器视觉验收。

回退参考：修改前主要文件备份在 `.runtime/backups/20260912-ui-controls-9873e3ef/`；本轮新增模块及前端文件可从本记录定位。未修改旧项目。

测试后对 iCloud 主目录做了只读扫描：共 660 项，其中 143 个 __pycache__ 命名目录为较早创建；今天创建的根目录项包括 .obsidian、attachment、Dairy、homepage。这些目录的来源和依赖尚未逐项核实，本轮未删除任何云盘文件，不能认定它们由本轮修改产生。
